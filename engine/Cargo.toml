[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbeam = "0.8"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "order_book"
harness = false

[lib]
name = "engine"
path = "src/lib.rs"